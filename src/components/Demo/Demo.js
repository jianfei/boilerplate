import './Demo.less';

export default class Demo extends React.Component {
    static defaultProps = {
    };

    state = {
    };

    render() {
        return (
            <div className="compo-demo" />
        );
    }
}
